{
  "name": "example-express",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/client.js --bundle --format=esm --outfile=public/client.js",
    "dev": "npm run build && node --env-file=.env --watch server.js",
    "start": "npm run build && node --env-file=.env server.js"
  },
  "dependencies": {
    "@preprio/toolkit": "workspace:*",
    "ejs": "^3.1.10",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
